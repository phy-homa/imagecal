投稿ページ
<%= form_with model: @image, local: true do |form| %>
  <%= form.text_area :comment, placeholder: "投稿者コメント欄", rows: "3"%>
  <%= form.submit "SEND" %>
<% end %>
・画像追加
・タグ追加
・メッセージ追加
・投稿ボタン

・userと紐付けして投稿者のnameをえる
