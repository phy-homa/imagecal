投稿ページ
<%= form_with model: @image, url: images_path, local: true do |f| %>
<div class="form">
  <div class="picture-form">
    <%= f.label :picture, "イラスト" %><br />
    <%= f.file_field :picture, id: "image_picture" %>
  </div>
  <div class="comment-form">
    <%= f.label :comment, "投稿者コメント" %><br />
    <%= f.text_area :comment, placeholder: "投稿者コメント欄", rows: "3", class:"comment-box"%>
  </div>
  <div class="tag-form">
    <%= f.label :name, "タグ" %><br />
    <%= f.text_field :name, class:"tag-box" %>
  </div>
  <div class="submit-form">
    <%= f.submit "SEND", class:"submit-btn" %>
  </div>
</div>
<% end %>
